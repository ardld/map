{
  "name": "photo-map",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node build-photo-geojson.mjs && cp -f public/index.html public/200.html || true",
    "dev": "npx serve public -p 3000"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "dropbox": "^10.34.0",
    "exiftool-vendored": "^28.3.0",
    "jimp": "^0.22.12",
    "node-fetch": "^3.3.2"
  }
}
